*.csv
creds/
groups/
groups.json
metrics.jsonl
proxy.log
koto.sock
run/
.gocache/
.gomodcache/
.build/
/daemon/daemon
/daemon/koto
/koto
/koto-static
/koto-tui
/tui/koto-tui
schedules.json
goals.json
__pycache__/
*.pyc
tui/node_modules/
fcassets
fcguest/fc-agent
fcguest/koto-fcagent
/koto-fcagent
.kernelcache/
/.cargocache
/.claude
# `claude auth login` with HOME=<clone> writes this next to .claude/ — no
# tokens in it, but it carries oauthAccount/userID.
/.claude.json
# `make dev` state dir: a second daemon's groups/creds/assets, beside an
# installed koto. Guest assets in it are hardlinks to fcassets/.
/.dev
.koto-fetch.*/

# personal todo list, never part of the project
todo.txt

# release build output (make dist); nothing here is checked in
/dist/
